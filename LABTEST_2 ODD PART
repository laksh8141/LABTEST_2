import pandas as pd
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_absolute_error, r2_score

diabetes_data = load_diabetes()
x=diabetes_data.data
y=diabetes_data.

l=LinearRegression()
l.fit=(x_train,y_train);
y_pred_l=l.predict(x_test)
scaler=standardscaler()
x_train_scaled = scalar_fit_transfrom(x_train_fit)
x_test_scaled = scalar_fit_transfrom(x_test_fit)

a_lr = accuracy_score(y_test,y_pred_1.round)

l=LinearRegression()
l.fit=(x_blood,y_blood);
y_pred_l=l.predict(x_test)
scaler=standardscaler()
x_blood_scaled = scalar_fit_transfrom(x_blood_fit)
x_test_scaled = scalar_fit_transfrom(x_test_fit)
svr=svm()

svm_fit=(x_blood,y_blood)
a_lr = accuracy_score(y_test,y_pred_1.round)
a_svm=accuracy_score(y_test,y_pred,y_)
